class Viewport{
    //Hay que hacer uno predefinido
    constructor(x, y, alto, ancho){
        this.x=x;
        this.y=y;
        this.alto=alto;
        this.ancho=ancho;
    }
    setValuesViewport(x, y, alto, ancho){
        this.x=x;
        this.y=y;
        this.alto=alto;
        this.ancho=ancho;
    }
}
